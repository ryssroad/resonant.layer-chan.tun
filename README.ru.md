# 🌀 Resonant Protocol

**Связь между моделями через передачу latent-состояний**

Resonant Protocol обеспечивает прямую коммуникацию между нейронными сетями через передачу скрытых состояний, KV-кэшей и эмбеддингов. Построен на основе спецификации Kimi K2 с расширениями для верификации embedding space, critique-сообщений и гибких типов данных.

---

## 🎯 Возможности

- **Бинарный формат V-Frame** — компактные фреймы с контрольными суммами (≤64 КиБ)
- **Передача latent-состояний** — отправка скрытых представлений между моделями
- **Разделение KV-кэша** — эффективная передача attention cache
- **Critique-сообщения** — обратная связь о расхождениях между моделями
- **Верификация embedding space** — гарантия совместимости представлений
- **Сжатие и шифрование** — Zstd + XChaCha20-Poly1305
- **Множество dtype** — F16, I8, Q4, Sparse COO
- **Мультимодальность** — текст, изображения, аудио, графы, смешанное

---

## 📦 Структура репозитория

```
resonant-protocol/
├── README.md                        # English
├── README.ru.md                     # Русский
├── PITCH.ru.md                      # Питчфорк проекта
├── specs/
│   ├── vframe_binary_layout.md      # Спецификация бинарного формата
│   └── capability_handshake.md      # Протокол handshake
├── rust/
│   ├── Cargo.toml
│   └── src/
│       ├── lib.rs                   # Ядро V-Frame
│       ├── dtype.rs                 # Типы данных и модальности
│       ├── compress.rs              # Сжатие Zstd
│       ├── crypto.rs                # XChaCha20-Poly1305
│       └── examples/
│           ├── send_think.rs        # UDP отправитель
│           └── recv_think.rs        # UDP получатель
└── python/
    ├── send_think.py                # Python отправитель
    └── recv_think.py                # Python получатель
```

---

## 🚀 Быстрый старт

### Rust-реализация

```bash
cd rust

# Сборка библиотеки
cargo build --release

# Запуск получателя (в одном терминале)
cargo run --example recv_think

# Запуск отправителя (в другом терминале)
cargo run --example send_think
```

### Python-реализация

```bash
cd python

# Запуск получателя
python recv_think.py

# В другом терминале, отправка сообщений
python send_think.py
```

---

## 💡 Примеры использования

### Rust: Отправка Think-сообщения

```rust
use resonant_protocol::*;

let frame = VFrame {
    hdr: VFrameHeader {
        version: 1,
        mtype: MsgType::Think,
        flags: Flags::ZSTD,
        stream_id: 0x1234,
        frame_seq: 1,
        num_slices: 1,
        slice_len: vec![4096],
        space_hash32: 2451163210,
        modality: Modality::Text,
    },
    slices: vec![(
        SliceMeta {
            dtype: DType::F16,
            shape: vec![1, 2048],
        },
        vec![0u8; 4096], // Ваше скрытое состояние
    )],
    crc32: 0,
};

let bytes = frame.encode()?;
// Отправить bytes через UDP/TCP/WebRTC
```

### Python: Отправка Critique-сообщения

```python
from send_think import build_frame, MSG_CRITIQUE, DTYPE_F16

divergence = b"\x00" * 32
explanation = json.dumps({"note": "расхождение на 12-м слое"}).encode()

frame = build_frame(
    msg_type=MSG_CRITIQUE,
    stream_id=0x5678,
    frame_seq=3,
    slices=[
        {"dtype": DTYPE_F16, "shape": [16], "payload": divergence},
        {"dtype": DTYPE_I8, "shape": [len(explanation)], "payload": explanation}
    ]
)

sock.sendto(frame, ("127.0.0.1", 50051))
```

---

## 🧪 Типы сообщений

### Think (0)
**Назначение:** Передача latent-состояний / скрытых представлений

**Срезы:**
- `[0]`: Скрытое состояние (f16/i8/q4)

**Пример:** После обработки текста модель A отправляет своё скрытое состояние модели B для продолжения генерации.

### Cache (1)
**Назначение:** Разделение KV-кэша для attention

**Срезы:**
- `[0]`: Ключи (f16)
- `[1]`: Значения (f16)

**Пример:** Передача кэшированного attention для эффективного многоходового диалога.

### Ask (2)
**Назначение:** Запрос или обращение к другой модели

**Срезы:**
- `[0]`: Query embedding
- `[1]` (опционально): Контекст

### Sync (3)
**Назначение:** Управляющие сообщения, capability handshake

**Срезы:**
- `[0]`: JSON payload (ping/capability/error)

**Пример:** Согласование совместимости embedding space перед передачей данных.

### Critique (4)
**Назначение:** Отправка обратной связи о расхождениях или исправлений

**Срезы:**
- `[0]`: Вектор расхождения (f16)
- `[1]` (опционально): Маска проблемных измерений
- `[2]` (опционально): Человекочитаемое объяснение (JSON)

**Пример:** Модель B обнаруживает семантический дрейф и отправляет сигнал коррекции модели A.

---

## 🔐 Безопасность и сжатие

### Zstd-сжатие

Включается через `Flags::ZSTD`:

```rust
// Rust
use resonant_protocol::compress::*;
let compressed = zstd_compress(&data, 3)?;
```

```python
# Python
import zstd
compressed = zstd.compress(data, 3)
```

### XChaCha20-Poly1305 шифрование

Включается через `Flags::XCHACHA`:

```rust
// Rust
use resonant_protocol::crypto::*;
let key = [42u8; 32];
let nonce = [13u8; 24];
let ciphertext = seal_xchacha(&key, &nonce, &plaintext)?;
```

---

## 🧩 Расширения Kimi K2

### 1. Верификация Embedding Space

**Проблема:** Модели с разными архитектурами производят несовместимые эмбеддинги.

**Решение:** Включение `space_hash32` в заголовок и capability handshake.

```rust
VFrameHeader {
    space_hash32: 2451163210, // Вычислен из сигнатуры модели
    // ...
}
```

Получатели отклоняют фреймы с несовпадающими хэшами.

### 2. Critique-сообщения

**Проблема:** Нет механизма обратной связи для расхождений или семантического дрейфа.

**Решение:** Новый тип сообщения (4) для исправлений между моделями.

```json
{
  "note": "расхождение в измерениях [3, 17]",
  "magnitude": 0.042,
  "suggested_correction": "увеличить внимание к токену 15"
}
```

### 3. Расширенные DType

| DType | K2 | Resonant |
|-------|:--:|:--------:|
| F16   | ✅ | ✅       |
| I8    | ❌ | ✅       |
| Q4    | ❌ | ✅       |
| SparseCoo | ❌ | ✅   |

Позволяет коммуникацию квантованных и разреженных моделей.

### 4. Поле Modality

Поддержка мультимодальных моделей:
- Text (0)
- Image (1)
- Audio (2)
- Graph (3)
- Mixed (4)

### 5. Strong Tail Hash

`xxhash3_64` для верификации всего потока:

```rust
let stream_hash = strong_tail_hash(&all_frames);
// Включить в TAIL frame
```

---

## 📚 Спецификации

- **[V-Frame Binary Layout](specs/vframe_binary_layout.md)** — Полная справка по формату
- **[Capability Handshake](specs/capability_handshake.md)** — Протокол согласования

---

## 🛠️ Разработка

### Запуск тестов

```bash
cd rust
cargo test
```

### Сборка в release

```bash
cargo build --release
# Бинарник в target/release/
```

---

## 🗺️ Дорожная карта

- [x] Кодирование/декодирование V-Frame
- [x] UDP-примеры (Rust + Python)
- [x] Сжатие (zstd)
- [x] Шифрование (XChaCha20-Poly1305)
- [x] Critique-сообщения
- [x] Верификация embedding space
- [ ] TCP transport layer
- [ ] Адаптер WebRTC data channel
- [ ] FEC (Reed-Solomon)
- [ ] Адаптивная квантизация (on-the-fly Q4)
- [ ] Мультиплексирование потоков
- [ ] QUIC-based transport

---

## 📖 Сценарии использования

### 1. Распределённый инференс

Разделение большой модели на несколько машин:
- Машина A: Слои 1-12
- Машина B: Слои 13-24

```
A: Вход → Think(слои 1-12) → [latent state]
   [UDP передача]
B: Think(слои 13-24) → Выход
```

### 2. Ансамбль моделей

Несколько специализированных моделей сотрудничают:

```
Vision Model → Think(image features)
   ↓
Text Model → Think(подпись + контекст)
   ↓
Fusion Model → Финальный вывод
```

### 3. Непрерывное обучение

Модель A учится на исправлениях модели B:

```
A: Генерация вывода
B: Critique(расхождение в рассуждении)
A: Обновление на основе critique
```

### 4. Семантическая маршрутизация

Маршрутизация запросов к лучшей модели на основе embedding space:

```
Router: Ask(query embedding)
   ↓
Специализированная модель A/B/C: Think(ответ)
```

---

## 🤝 Вклад в проект

Приветствуются любые контрибуции! Области для исследования:

- Реализации transport layer
- Языковые привязки (Go, Julia, JavaScript)
- Интеграция с ML-фреймворками (PyTorch, JAX)
- FEC и коррекция ошибок
- Бенчмарки производительности

---

## 📄 Лицензия

MIT License — см. файл LICENSE

---

## 🙏 Благодарности

- Основан на [спецификации Kimi K2](https://github.com/MoonshotAI/Kimi-k2-protocol)
- Вдохновлён исследованиями в области model-to-model communication
- Создан для открытой AI-инфраструктуры

---

**Создано с ❤️ для будущего распределённого интеллекта**
